# Dependencies
node_modules/
# vendor/ is committed on purpose — phpseclib must ship in the plugin zip.

# Build artifacts (compiled Svelte bundles — built at release time, not committed)
assets/dist/
pro/assets/dist/
*.log

# Two-plugin zip output (scripts/make-zips.mjs)
dist/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Scratch
CLAUDE copy.md

# Plugin ZIP output
*.zip
